 # 限流配置
 ip:
  limited:
    # 时间窗大小（second）
    limit: 10
    # 访问次数
    time: 1000

## 邮件配置
  mail:
    username: fw940314@foxmail.com
    password: clzotjwzysgvbjaa
    host: smtp.qq.com
    properties:
      mail:
        smtp:
          ssl:
            enable: true
# database            
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springcloud?useUnicode=true&characterEncoding=utf8
    username: root
    password: 123456
  jpa:
    database: MySQL
    show-sql: true
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl  #避免将驼峰命名转换为下划线命名
